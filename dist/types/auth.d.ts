export declare type ConnectTokenOptions = {
    /** Url where notifications will be sent at any item's event */
    webhookUrl?: string;
    /** An unique identifier of the user, usually used the UserId of your app */
    clientUserId?: string;
};
